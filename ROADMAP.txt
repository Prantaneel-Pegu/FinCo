END TASKS:
    Create a template repo with basic settings pre-configured such as my .vscode, prettier config, tailwind config, eslint config etc.
    and other utilities such as one command git add, commit & upload etc.